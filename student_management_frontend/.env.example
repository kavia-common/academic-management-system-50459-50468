# Frontend environment example (do not commit real secrets)

# Preferred base URL for backend API
REACT_APP_API_BASE=https://your-backend.example.com

# Alternative variable name if you cannot set REACT_APP_API_BASE
# REACT_APP_BACKEND_URL=https://your-backend.example.com

# Health check path (optional, defaults to /health)
REACT_APP_HEALTHCHECK_PATH=/health

# Optional environment indicator
REACT_APP_NODE_ENV=development
